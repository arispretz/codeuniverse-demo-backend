# Environment type (test, development, production)
NODE_ENV=test

# Port where the backend server will run during tests
PORT=4000

# backend base URL used in tests
BACKEND_URL=http://localhost:5000

# Frontend base URL used in tests
FRONTEND_URL=http://localhost:3000

# MongoDB connection string (use test database or in-memory server)
MONGO_URI=mongodb://localhost:27017/testdb

# URL of the machine learning service (mocked for tests)
ML_SERVICE_URL=http://localhost:6000
ASSISTANT_URL=http://localhost:6000/reply

# Secret key for session handling (dummy value for tests)
SESSION_SECRET=super-secret-test-key

# Firebase credentials as JSON string (mocked values)
FIREBASE_CREDENTIAL_JSON={"project_id":"test-project","client_email":"test@test.com","private_key":"dummy-key"}

# Judge0 API URL (mocked for tests)
JUDGE0_URL=http://localhost:7000/judge0

# RapidAPI key (dummy value for tests)
RAPIDAPI_KEY=rapidapi_test_key

# Allowed CORS origins for socket connections (dummy localhost)
SOCKET_CORS_ORIGINS=http://localhost:3000

# Allowed origins for API requests (dummy localhost)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Local hostname used in tests
LOCAL_HOSTNAME=localhost

# Local IP address used in tests
LOCAL_IP=127.0.0.1

# Company domain (dummy value for tests)
COMPANY_DOMAIN=testcompany.local

# Seed user password (dummy value for tests)
SEED_USER_PASSWORD=seedpassword123
